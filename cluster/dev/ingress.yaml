---
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: illumino-ingress
spec:
  rules:
    - host: api.localhost
      http:
        paths:
          - path: /graphql
            backend:
              serviceName: frontend-api-service
              servicePort: 80
---

